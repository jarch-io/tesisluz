import React, {Fragment} from 'react';
import classnames from 'classnames';

import MyRequest from '../../Elements/Requests/MyRequest';

const RequestElement = ({match}) => (
	<MyRequest role="adviser" />
);

export default RequestElement;